version: "3.8"

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: splitstack
      POSTGRES_PASSWORD: splitstack
      POSTGRES_DB: splitstack
    ports:
      - "5432:5432"
    volumes:
      - splitstack_data:/var/lib/postgresql/data

volumes:
  splitstack_data:
